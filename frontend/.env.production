# Production environment — committed to repo (non-sensitive values only)
# After your first Vercel deploy, replace 'bucms' with your actual project name/domain.
# Backend is mounted at /_/backend on the same domain as the frontend.
NEXT_PUBLIC_API_URL=https://bucms.vercel.app/_/backend
